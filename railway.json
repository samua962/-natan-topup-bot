{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && cd admin-dashboard && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "node index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}