[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build", "test -f admin-dashboard/build/index.html"]

[phases.deploy]
cmd = "node index.js"